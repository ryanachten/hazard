version: "2"
run:
  timeout: 2m
linters:
  enable:
    - govet
    - errcheck
    - staticcheck
    - ineffassign
    - unused
    - misspell
    - revive
  exclusions:
    generated: lax
formatters:
  enable:
    - gofmt
    - goimports
  settings:
    gofmt:
      simplify: true
